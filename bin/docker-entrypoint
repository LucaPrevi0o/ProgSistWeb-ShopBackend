#!/bin/bash -e

# If the application server is being started (directly or through Thruster),
# create/migrate the persistent SQLite databases before accepting requests.
if [[ " $* " == *" ./bin/rails server "* ]]; then
  ./bin/rails db:prepare
fi

exec "${@}"
